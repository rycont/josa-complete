{
  "name": "josa-complete",
  "version": "2.0.1",
  "description": "한국어 조사를 완성시켜줍니다",
  "main": "./dist/index.js",
  "devDependencies": {
    "@types/node": "^14.0.27",
    "jest": "^26.0.1",
    "jest-in-case": "^1.0.2",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "gen": "gocog -c=node -a=%s -a=-p ./src/**.ts && ts-standard --fix",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "$npm_execpath run build && jest",
    "test:watch": "$npm_execpath run build:watch & jest --watchAll"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rycont/josa-complete.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rycont/josa-complete/issues"
  },
  "homepage": "https://github.com/rycont/josa-complete#readme"
}
